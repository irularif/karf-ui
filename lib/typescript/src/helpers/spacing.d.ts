import type { TDevice } from '../ScreenProvider/context';
export declare type ThemeSpacing = Record<TDevice, number>;
export declare const defaultSpacing: {
    xs: number;
    sm: number;
    md: number;
    lg: number;
    xl: number;
};
