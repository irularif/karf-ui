import { RNFunctionComponent } from '../helpers';
import { ViewProps } from '../View';
export interface TopBarProps extends ViewProps {
    disableShadow?: boolean;
}
export declare const TopBarBase: RNFunctionComponent<TopBarProps>;
